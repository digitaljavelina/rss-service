---
phase: 05-automation-scheduling
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - README.md
autonomous: false
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04

must_haves:
  truths:
    - "User has verified feeds auto-refresh on schedule"
    - "User has verified dashboard shows refresh timing"
    - "README.md reflects Phase 5 features"
  artifacts:
    - path: "README.md"
      provides: "Updated project documentation with Phase 5 features"
      contains: "auto-refresh"
  key_links: []
---

<objective>
Update README.md with Phase 5 features and verify all automation capabilities with the user.

Purpose: Satisfy the phase completion rule (README update) and ensure scheduled refreshes work correctly in production.

Output: Updated README.md and user verification of Phase 5 features.
</objective>

<execution_context>
@/Users/michaelhenry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelhenry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-automation-scheduling/05-01-SUMMARY.md
@.planning/phases/05-automation-scheduling/05-02-SUMMARY.md
@.planning/phases/05-automation-scheduling/05-03-SUMMARY.md
@.planning/phases/05-automation-scheduling/05-04-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with Phase 5 features</name>
  <files>README.md</files>
  <action>
    Update README.md to reflect Phase 5 features:

    1. Update features list to add:
       - **Auto-Refresh** - Feeds update automatically on configurable schedules (15min, 30min, hourly, daily)
       - **Refresh Status** - Dashboard shows last updated time, next refresh, and status for each feed

    2. Update API documentation:
       - POST /api/feeds now accepts `refresh_interval_minutes`
       - PUT /api/feeds/:id now accepts `refresh_interval_minutes`
       - GET /api/feeds returns refresh timing fields

    3. Update project status to show Phase 5 complete

    4. Add note about Vercel cron requirements:
       - Pro plan required for per-minute cron jobs
       - CRON_SECRET environment variable required
  </action>
  <verify>
    - `grep -c "auto-refresh" README.md` returns at least 1
    - README.md mentions refresh interval configuration
    - Project status shows Phase 5 complete
  </verify>
  <done>
    README.md updated with Phase 5 features
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 5 — Automation & Scheduling</name>
  <files>none</files>
  <action>
    Phase 5 delivers automatic feed refreshes. Deploy to Vercel first, then verify:

    **Test 1: Create feed with auto-refresh (SCHED-03)**
    1. Go to /create
    2. Enter a URL and select "Every 15 minutes" refresh interval
    3. Preview and create the feed
    4. Verify feed is created with next_refresh_at set ~15 minutes from now

    **Test 2: Dashboard shows refresh timing (SCHED-04)**
    1. Go to /feeds dashboard
    2. Verify "Last Updated" column shows relative time
    3. Verify "Next Refresh" column shows time until next refresh
    4. Verify status column shows feed status

    **Test 3: Edit feed refresh interval (SCHED-03)**
    1. Click Edit on a feed
    2. Change refresh interval to a different value
    3. Save and verify next_refresh_at is recalculated

    **Test 4: Cron job runs (SCHED-01, SCHED-02)**
    1. Create or update a feed with short interval (15 min)
    2. Wait for cron to run (check Vercel Functions logs)
    3. Verify feed's updated_at changes after cron runs
    4. Verify next_refresh_at is updated to future time

    **Test 5: Manual-only option**
    1. Create or edit a feed with "Manual only" option
    2. Verify next_refresh_at is null
    3. Verify feed does not get auto-refreshed by cron

    **Test 6: README**
    1. Verify README mentions auto-refresh and refresh intervals

    Resume signal: Type "approved" if all tests pass, or describe any issues found.
  </action>
  <verify>User confirms all 6 tests pass or reports issues for remediation</verify>
  <done>All Phase 5 features verified by user: auto-refresh, refresh intervals, dashboard timing</done>
</task>

</tasks>

<verification>
- All Phase 5 requirements covered (SCHED-01 through SCHED-04)
- README.md updated per phase completion rule
- User has verified auto-refresh, intervals, and dashboard timing
</verification>

<success_criteria>
- User confirms cron job runs and refreshes feeds automatically
- User confirms dashboard shows refresh timing information
- User confirms refresh interval is configurable per feed
- README.md is updated with Phase 5 features
</success_criteria>

<output>
After completion, create `.planning/phases/05-automation-scheduling/05-05-SUMMARY.md`
</output>
