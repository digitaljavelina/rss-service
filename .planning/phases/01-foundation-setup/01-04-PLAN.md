---
phase: 01-foundation-setup
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User confirms home page loads correctly"
    - "User confirms sidebar navigation is visible"
    - "User confirms theme toggle works"
    - "User confirms clean/minimal aesthetic matches expectations"
    - "User confirms feed endpoint returns valid RSS"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 1 foundation: web UI, database persistence, and feed serving.

Purpose: Before moving to Phase 2 (Core Feed Creation), confirm the foundation works correctly and matches the user's vision for the UI aesthetic.

Output: User approval to proceed to Phase 2, or list of issues to address.
</objective>

<execution_context>
@/Users/michaelhenry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelhenry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-setup/01-CONTEXT.md
@.planning/phases/01-foundation-setup/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 1 Foundation</name>
  <what-built>
Complete Phase 1 foundation including:
- Express server with auto-port selection
- SQLite database with feeds/items schema
- Feed serving at /feeds/:identifier with RSS 2.0 and Atom support
- Web UI with sidebar navigation and theme toggle
- Clean, minimal aesthetic inspired by Linear/Notion
  </what-built>
  <how-to-verify>
**1. Start the server:**
```bash
cd /Users/michaelhenry/Documents/Projects/RSS-Service
npm run dev
```
Note the port number in the console output.

**2. Check the home page:**
- Open http://localhost:3000 (or the port shown) in your browser
- Verify the sidebar is visible on the left with navigation links
- Verify the home page has a welcoming message and "Create Feed" CTA
- Check if the aesthetic feels "clean and minimal" like Linear/Notion

**3. Test theme toggle:**
- Find and click the theme toggle (should be in sidebar)
- Confirm the page switches between light and dark mode
- Refresh the page - theme should persist

**4. Test feed serving:**
Insert a test feed and verify RSS output:
```bash
sqlite3 data/rss-service.db "INSERT INTO feeds (id, slug, name, url) VALUES ('demo1', 'demo-feed', 'Demo Feed', 'https://example.com');"
sqlite3 data/rss-service.db "INSERT INTO items (id, feed_id, title, link, description, pub_date, guid) VALUES ('item1', 'demo1', 'Test Article', 'https://example.com/article', 'A test article', datetime('now'), 'unique-guid-1');"
```
Then open http://localhost:3000/feeds/demo-feed in browser:
- Should display XML (RSS format)
- Content-Type should be application/rss+xml

**5. Verify database persistence:**
- Stop the server (Ctrl+C)
- Start it again: `npm run dev`
- Open http://localhost:3000/feeds/demo-feed - feed should still work (data persisted)

**6. Check browser compatibility:**
- If possible, open in Chrome, Firefox, and/or Safari
- Confirm the UI renders correctly in each
  </how-to-verify>
  <resume-signal>
Type one of:
- "approved" - Everything works, ready for Phase 2
- "approved with notes: [your notes]" - Works but note some preferences
- Describe specific issues that need fixing before proceeding
  </resume-signal>
</task>

</tasks>

<verification>
User has verified:
1. Home page loads with correct layout
2. Sidebar navigation is visible and styled
3. Theme toggle works and persists
4. Feed endpoint serves valid RSS XML
5. Data persists across server restarts
6. UI aesthetic matches expectations (clean/minimal like Linear/Notion)
</verification>

<success_criteria>
- User approves the foundation
- All Phase 1 requirements verified:
  - DATA-01: SQLite storage working
  - DATA-02: Persistence across restarts
  - DATA-03: Item limit configured (default 100)
  - OUT-01: Feeds served at localhost URLs
  - OUT-02: Proper Content-Type headers
  - OUT-04: Caching headers present
  - UI-01: Clean, uncluttered UI
  - UI-04: Works in modern browsers
</success_criteria>

<output>
After user approval, create `.planning/phases/01-foundation-setup/01-04-SUMMARY.md` noting verification outcome.
</output>
