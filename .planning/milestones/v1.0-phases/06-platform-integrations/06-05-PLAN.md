# Plan 06-05: Scheduler Updates + README + Verification

## Goal
Update the cron scheduler to handle YouTube and Reddit feed types, update README with Phase 6 features, and verify all platform integrations end-to-end.

## Tasks

### 1. Scheduler — Platform-Aware Refresh
File: `api/cron/scheduler.ts` (update)

Update `refreshFeed()` to route by `feed_type`:
- `'web'` — existing flow (fetch page, auto-detect, extract)
- `'youtube'` — fetch API key from settings, call YouTube service
- `'reddit'` — fetch Reddit RSS, parse entries

Add `feed_type` and `platform_config` to the FeedRow interface in scheduler.
Import YouTube and Reddit services dynamically (same pattern as existing imports).

### 2. Scheduler — Query Includes Platform Feeds
Update the scheduler query to include YouTube and Reddit feeds.
Currently filters `url IS NOT NULL` — YouTube feeds may have null URL
(the source is the API, not a web URL). Update filter:
- Include feeds where `feed_type != 'web'` even if URL handling differs
- Or ensure YouTube/Reddit feeds always store a URL (the source URL)

### 3. README Update
Update README.md to reflect Phase 6 features:
- Platform integrations section (YouTube, Reddit)
- Settings / API key configuration
- Updated "How It Works" section
- Note about Twitter/X not being supported (and why)

### 4. Human Verification Checklist
End-to-end testing:

**YouTube Flow:**
- [ ] Configure YouTube API key in Settings page
- [ ] Create feed from a YouTube channel URL
- [ ] Preview shows recent videos
- [ ] Feed is created with feed_type='youtube'
- [ ] RSS URL serves valid XML with video items
- [ ] Manual refresh finds new videos
- [ ] Auto-refresh works via cron

**Reddit Flow:**
- [ ] Create feed from a subreddit URL (no API key needed)
- [ ] Preview shows recent posts
- [ ] Feed is created with feed_type='reddit'
- [ ] RSS URL serves valid XML with Reddit posts
- [ ] Manual refresh finds new posts
- [ ] Auto-refresh works via cron

**Settings:**
- [ ] Settings page accessible from sidebar
- [ ] YouTube API key can be saved, masked, and tested
- [ ] Missing API key shows clear error on YouTube feed creation

**Dashboard:**
- [ ] YouTube feeds show platform indicator
- [ ] Reddit feeds show platform indicator
- [ ] All feed types work in dashboard (refresh, edit, delete, export)

**Backwards Compatibility:**
- [ ] Existing web feeds still work unchanged
- [ ] Existing scheduled refreshes still work

## Verification
- [ ] Cron handles all three feed types
- [ ] README reflects current feature set
- [ ] All human verification items pass
