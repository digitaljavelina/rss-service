---
phase: 04-advanced-extraction
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - README.md
autonomous: false
requirements:
  - CORE-06
  - CORE-07
  - CORE-08

must_haves:
  truths:
    - "User has verified headless browser toggle works on a JS-heavy site"
    - "User has verified selector adjustment panel shows detected selectors and re-preview works"
    - "README.md reflects Phase 4 features"
  artifacts:
    - path: "README.md"
      provides: "Updated project documentation with Phase 4 features"
      contains: "headless"
  key_links: []
---

<objective>
Update README.md with Phase 4 features and verify all advanced extraction capabilities with the user.

Purpose: Satisfy the phase completion rule (README update) and ensure headless browser, selector adjustment, and auto-detection all work correctly in production.

Output: Updated README.md and user verification of Phase 4 features.
</objective>

<execution_context>
@/Users/michaelhenry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelhenry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-extraction/04-01-SUMMARY.md
@.planning/phases/04-advanced-extraction/04-02-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with Phase 4 features</name>
  <files>README.md</files>
  <action>
    Update README.md to reflect the current project status after Phase 4. Add/update sections:

    1. Update the project status/progress section to show Phase 4 complete
    2. Add Phase 4 features to the features list:
       - **Headless Browser Support:** Opt-in headless browser mode for JavaScript-heavy sites using @sparticuz/chromium. Toggle on the create feed page enables browser-based fetching for SPAs and dynamic content.
       - **JS-Need Detection:** Automatic detection of pages that likely need JavaScript rendering, with a suggestion banner when static extraction finds minimal content.
       - **Selector Adjustment Panel:** After preview, users can view and edit the auto-detected CSS selectors (item container, title, link, description, date) and re-preview with adjusted selectors.
       - **Auto-Detection (Phase 2):** Already-existing auto-detection of content patterns continues as the primary extraction path.
    3. Update API documentation if a section exists:
       - POST /api/preview now accepts `useHeadless: boolean` and returns `suggestHeadless: boolean`
       - POST /api/feeds now accepts `useHeadless: boolean` (stored in selectors for refresh)
    4. Note the Vercel deployment config change (memory: 1024 MB, maxDuration: 60s)

    Keep README concise and user-focused. Do not add implementation details — just features and API changes.
  </action>
  <verify>
    - `grep -c "headless" README.md` returns at least 2
    - `grep -c "selector" README.md` returns at least 1
    - README.md exists and is not empty
  </verify>
  <done>
    README.md updated with Phase 4 features including headless browser support, JS-need detection, and selector adjustment panel
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 4 — Advanced Extraction</name>
  <files>none</files>
  <action>
    Phase 4 delivers three capabilities. Deploy to Vercel first (if not auto-deployed), then have user verify:

    1. **Headless browser support (CORE-08):** Toggle on create feed page enables browser-based fetching via @sparticuz/chromium for JS-heavy sites
    2. **Selector adjustment panel (CORE-06):** After preview, expandable panel shows auto-detected selectors as editable inputs with re-preview capability
    3. **Auto-detection (CORE-07):** Already complete from Phase 2 — auto-detect continues to work as primary extraction path

    **Test 1: Auto-detection still works (CORE-07 — regression check)**
    1. Go to /create
    2. Enter a known working URL (e.g., https://news.ycombinator.com)
    3. Click Preview — confirm items are detected and displayed
    4. Confirm the "Adjust Selectors" panel appears with pre-filled selector values

    **Test 2: Selector adjustment panel (CORE-06)**
    1. After successful preview, expand the "Adjust Selectors" panel
    2. Verify the selector inputs are pre-filled with detected values (item, title, link, etc.)
    3. Modify a selector value, click "Re-preview with these selectors"
    4. Confirm the preview updates with the adjusted selectors

    **Test 3: Headless browser toggle (CORE-08)**
    1. On /create, check the "Use headless browser" checkbox
    2. Enter a URL and click Preview
    3. Confirm items are detected (headless may be slower, 3-10s for cold start)

    **Test 4: JS-need suggestion (CORE-08)**
    1. Ensure headless toggle is OFF, enter a known SPA URL with minimal static HTML
    2. Click Preview — if minimal static content, confirm suggestion banner appears

    **Test 5: Feed creation with headless (CORE-08)**
    1. Create a feed with headless toggle ON
    2. Go to /feeds dashboard, click Refresh — confirm it works with headless

    **Test 6: README**
    1. Check that README.md mentions headless browser support and selector adjustment

    Resume signal: Type "approved" if all tests pass, or describe any issues found.
  </action>
  <verify>User confirms all 6 tests pass or reports issues for remediation</verify>
  <done>All Phase 4 features verified by user: headless browser, selector adjustment, auto-detection regression, README updated</done>
</task>

</tasks>

<verification>
- All Phase 4 requirements covered (CORE-06, CORE-07, CORE-08)
- README.md updated per phase completion rule
- User has verified headless browser, selector adjustment, and auto-detection
</verification>

<success_criteria>
- User confirms headless browser toggle works for JS-heavy sites
- User confirms selector adjustment panel shows detected selectors and re-preview works
- User confirms auto-detection continues to work (regression check)
- README.md is updated with Phase 4 features
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-extraction/04-03-SUMMARY.md`
</output>
