---
phase: 01-foundation-setup
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/views/layouts/main.html
  - src/views/pages/home.html
  - src/views/partials/sidebar.html
  - src/routes/ui.ts
  - public/css/styles.css
  - public/js/app.js
  - tailwind.config.js
autonomous: true

must_haves:
  truths:
    - "User can navigate to localhost and see the home page"
    - "Sidebar is always visible with navigation links"
    - "User can toggle between light and dark mode"
    - "Theme preference persists across page refresh"
    - "UI has clean, minimal aesthetic (like Linear/Notion)"
  artifacts:
    - path: "src/views/layouts/main.html"
      provides: "Base HTML layout with theme support"
      contains: "dark"
    - path: "src/views/pages/home.html"
      provides: "Home page with quick-start guidance"
      min_lines: 20
    - path: "src/views/partials/sidebar.html"
      provides: "Sidebar navigation component"
      contains: "menu"
    - path: "public/css/styles.css"
      provides: "Compiled Tailwind CSS"
      min_lines: 100
    - path: "tailwind.config.js"
      provides: "Tailwind configuration with daisyUI"
      contains: "daisyui"
  key_links:
    - from: "src/routes/ui.ts"
      to: "src/views/pages/home.html"
      via: "res.sendFile or template render"
      pattern: "sendFile|render"
    - from: "src/views/layouts/main.html"
      to: "public/css/styles.css"
      via: "link stylesheet"
      pattern: "href.*styles.css"
    - from: "src/views/layouts/main.html"
      to: "public/js/app.js"
      via: "script src"
      pattern: "src.*app.js"
---

<objective>
Create the web UI shell with sidebar navigation, theme toggle (light/dark), and home page with quick-start guidance.

Purpose: This establishes the visual foundation users will interact with. The clean, minimal aesthetic (like Linear/Notion) and persistent sidebar provide consistent navigation as features are added.

Output: Working web UI with sidebar, theme toggle, and welcoming home page that guides users toward creating their first feed.
</objective>

<execution_context>
@/Users/michaelhenry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelhenry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
@.planning/phases/01-foundation-setup/01-CONTEXT.md
@.planning/phases/01-foundation-setup/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Tailwind CSS with daisyUI and create base layout</name>
  <files>
    - tailwind.config.js
    - src/styles/input.css
    - public/css/styles.css
    - src/views/layouts/main.html
    - src/views/partials/sidebar.html
    - package.json (add build:css script)
  </files>
  <action>
Set up the CSS infrastructure and base layout:

1. Create `tailwind.config.js`:
   ```javascript
   /** @type {import('tailwindcss').Config} */
   export default {
     content: ['./src/views/**/*.html', './public/js/**/*.js'],
     darkMode: 'selector',
     theme: { extend: {} },
     plugins: [require('daisyui')],
     daisyui: {
       themes: ['light', 'dark'],
       darkTheme: 'dark',
     }
   }
   ```

2. Create `src/styles/input.css`:
   ```css
   @import 'tailwindcss';
   ```

3. Add scripts to package.json:
   - "build:css": "npx tailwindcss -i ./src/styles/input.css -o ./public/css/styles.css"
   - "dev:css": "npx tailwindcss -i ./src/styles/input.css -o ./public/css/styles.css --watch"
   - Update "dev" to run both: "concurrently \"npm run dev:css\" \"tsx watch src/server.ts\""

4. Install concurrently: `npm install -D concurrently`

5. Create `public/css/` directory and run initial build: `npm run build:css`

6. Create `src/views/layouts/main.html`:
   - DOCTYPE html with lang="en"
   - Head with meta charset, viewport, title "RSS Service"
   - Inline script in head (BEFORE body) to prevent theme flash:
     ```javascript
     (function() {
       const theme = localStorage.getItem('theme') ||
         (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
       document.documentElement.setAttribute('data-theme', theme);
       if (theme === 'dark') document.documentElement.classList.add('dark');
     })();
     ```
   - Link to /css/styles.css
   - Body with flex layout: sidebar (fixed width 256px) + main content area
   - Include Alpine.js from CDN or public/js
   - Include /js/app.js
   - Use daisyUI bg-base-100 for body, bg-base-200 for sidebar

7. Create `src/views/partials/sidebar.html`:
   - aside element with w-64, min-h-screen, bg-base-200
   - Logo/title at top: "RSS Service" in text-xl font-bold
   - Navigation menu using daisyUI menu classes:
     - Dashboard (links to /)
     - My Feeds (links to /feeds - placeholder for Phase 3)
     - Create Feed (links to /create - placeholder for Phase 2)
     - Settings (links to /settings - placeholder)
   - Theme toggle at bottom using daisyUI swap component or custom Alpine.js toggle
   - Use daisyUI's menu, menu-title, and active state classes
  </action>
  <verify>
1. Run `npm run build:css` - should create public/css/styles.css
2. Check styles.css exists and has content (should be large file with all Tailwind utilities)
3. Verify tailwind.config.js has daisyui plugin listed
4. Open main.html in browser directly - should show basic structure with daisyUI styling
  </verify>
  <done>
Tailwind CSS with daisyUI configured, base layout created with sidebar partial, theme toggle infrastructure in place, CSS builds correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create home page, UI routes, and theme toggle functionality</name>
  <files>
    - src/views/pages/home.html
    - src/routes/ui.ts
    - public/js/app.js
    - src/routes/index.ts (update to include UI routes)
    - src/app.ts (add view engine if needed)
  </files>
  <action>
Create the home page and wire up UI routes:

1. Create `src/views/pages/home.html`:
   - Extends main.html layout (or include via simple templating)
   - Main content area with:
     - Welcome heading: "Create RSS feeds from anything"
     - Subheading explaining the value prop
     - Quick-start card/section with prominent "Create Your First Feed" button/link
     - Brief explanation: "Point at any URL, select what content matters, get a feed"
     - Maybe 3 simple steps illustrated: 1. Enter URL, 2. Select content, 3. Subscribe
   - Use daisyUI card, hero, or similar components for clean look
   - Empty state should feel inviting, not empty

2. Create `public/js/app.js`:
   - Initialize Alpine.js global store for theme:
     ```javascript
     document.addEventListener('alpine:init', () => {
       Alpine.store('theme', {
         current: localStorage.getItem('theme') ||
           (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'),
         toggle() {
           this.current = this.current === 'dark' ? 'light' : 'dark';
           localStorage.setItem('theme', this.current);
           document.documentElement.setAttribute('data-theme', this.current);
           document.documentElement.classList.toggle('dark', this.current === 'dark');
         }
       });
     });
     ```
   - Import Alpine from CDN in main.html: `<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>`

3. Create `src/routes/ui.ts`:
   - Import Router, path, fs
   - Create uiRouter
   - Helper function to serve HTML by reading and combining layout + page
   - GET / serves home.html (combined with main layout + sidebar)
   - For now, use simple string concatenation or fs.readFileSync to combine:
     - Read main.html, replace {{content}} with page content
     - Read sidebar.html, inject into layout
   - Export uiRouter

4. Update `src/routes/index.ts`:
   - Import uiRouter from ./ui
   - Mount uiRouter at root level (use .use('/', uiRouter))
   - Ensure UI routes don't conflict with /feeds (order matters: specific routes first)

5. Update sidebar.html with working theme toggle:
   - Button with Alpine.js: `x-on:click="$store.theme.toggle()"`
   - Show sun/moon icon based on current theme
   - Use daisyUI swap or simple conditional rendering

For simple HTML serving without a template engine: Read files, do string replacement ({{sidebar}}, {{content}}), send response. Keep it simple - no need for EJS/Handlebars complexity.
  </action>
  <verify>
1. Start server: `npm run dev`
2. Open http://localhost:3000 in browser
3. Verify:
   - Home page loads with sidebar visible on left
   - Navigation links are present (Dashboard, My Feeds, Create Feed, Settings)
   - "Create Your First Feed" or similar CTA is prominent
   - Clean, minimal aesthetic (not default browser styling)
4. Test theme toggle:
   - Click theme toggle button
   - Page should switch between light and dark themes
   - Refresh page - theme should persist
5. Check mobile responsiveness (resize browser) - sidebar should remain visible
6. Verify no console errors in browser dev tools
  </verify>
  <done>
Home page displays with sidebar navigation, theme toggle works and persists, UI matches clean/minimal aesthetic like Linear/Notion, ready for user verification.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts both CSS watcher and server
2. http://localhost:3000 shows home page with sidebar
3. Theme toggle switches between light/dark
4. Theme persists after page refresh
5. Navigation links are visible and styled
6. No flash of wrong theme on page load
7. Responsive design works (sidebar remains visible)
8. daisyUI components render correctly (proper colors, spacing)
</verification>

<success_criteria>
- Web UI is clean, uncluttered, easy to navigate (UI-01)
- Works in modern browsers (UI-04)
- Sidebar always visible with navigation
- Light/dark mode with user toggle, persists across sessions
- Quick-start guidance visible on home page
- CSS builds correctly with Tailwind + daisyUI
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-03-SUMMARY.md`
</output>
