# Dependencies (rebuilt inside container)
node_modules/

# Build output (produced in builder stage)
dist/

# Environment files (secrets)
.env
.env.*
!.env.docker.example

# Git
.git/
.gitignore

# Logs and system files
*.log
.DS_Store

# Planning documentation
.planning/

# SQLite data directory (not needed in Docker)
data/

# Phase 7 dev-only compose (production compose is at root)
docker/
